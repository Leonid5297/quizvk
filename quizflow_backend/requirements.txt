Django==6.0.7
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
channels==4.3.2
daphne==4.2.2
django-cors-headers==4.9.0
django-filter==26.1
Pillow==12.1.1
python-dotenv==1.2.2
psycopg[binary]==3.2.3
requests==2.32.3
# Обязателен: docker-compose (см. корневой docker-compose.yml) всегда
# поднимает Redis и передаёт REDIS_URL бэкенду — без этого пакета кэш
# (quizzes/caching.py) падает с ModuleNotFoundError на первом же запросе.
# Нужен и при ручном локальном запуске, если сами задаёте REDIS_URL в .env.
redis==5.2.1

# Продакшен с несколькими репликами backend: раскомментируйте, чтобы
# WebSocket-события синхronизировались через тот же Redis, а не только
# in-memory одного процесса (см. CHANNEL_LAYERS в config/settings.py).
# channels_redis==4.2.1
