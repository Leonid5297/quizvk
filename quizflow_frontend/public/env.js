// В обычной локальной разработке (npm run dev) ничего не переопределяет —
// используются значения по умолчанию из src/App.jsx (localhost:8000/8100).
// В Docker-сборке этот файл на старте контейнера перезаписывается реальными
// адресами из переменных окружения (см. quizflow_frontend/docker-entrypoint.sh
// и nginx.conf) — пересобирать образ под каждый адрес бэкенда не нужно.
window.__QUIZVK_CONFIG__ = {};
